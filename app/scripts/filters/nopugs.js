'use strict';

/**
 * @ngdoc filter
 * @name statelessScoreboardApp.filter:noPugs
 * @function
 * @description
 * # noPugs
 * Filter in the statelessScoreboardApp.
 */
angular.module('statelessScoreboardApp')
  .filter('noPugs', function () {
    return function (input) {
      angular.foreach(items,function(item){
      })




      {record:{killer:'5428026242707973265'}};
    };
  });
//not used